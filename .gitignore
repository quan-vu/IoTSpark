# WordPress uploads / generated content
wp-content/uploads/
wp-content/upgrade/
wp-content/cache/

# Credentials (generated at runtime or server-only)
scripts/.api-credentials
.env.prod

# Docker volumes data (local only)
.docker/

# macOS
.DS_Store
